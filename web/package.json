{
  "name": "satisfactory-schematics-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "node scripts/sync-assets.mjs",
    "fetch-dist": "node scripts/fetch-dist.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro check && astro build",
    "build:netlify": "npm run fetch-dist && npm run build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/oswald": "^5.3.0",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.6.0"
  }
}
